# EIT-GUI (Flask Web App)

A simple Flask-based GUI for the Electrical Impedance Tomography (EIT) project.
Organized with `templates/` for HTML and `static/` for CSS, JavaScript, and images.

---

## 📂 Folder Structure
